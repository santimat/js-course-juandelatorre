const product = {
    name: "monitor 24'",
    price: 20000,
    available: true,
};

// return KEY from object
console.log(Object.keys(product));

// return values
console.log(Object.values(product));

// return all on arrays of pairs
console.log(Object.entries(product));
